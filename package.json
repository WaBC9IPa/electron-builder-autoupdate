{
  "name": "electron-auto-update-app",
  "version": "1.1.1",
  "description": "",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/WaBC9IPa/electron-builder-autoupdate.git"
  },
  "scripts": {
    "start": "electron .",
    "build": "rm -rf dist && electron-builder build --win --publish never",
    "deploy": "rm -rf dist && electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^8.5.2",
    "electron-builder": "^22.6.0"
  },
  "dependencies": {
    "electron-updater": "^4.3.1"
  }
}
